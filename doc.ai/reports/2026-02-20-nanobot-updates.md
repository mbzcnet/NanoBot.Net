# Nanobot 原项目更新报告

**报告日期**: 2026-02-20  
**更新范围**: 最近 4 天 (2026-02-16 ~ 2026-02-20)

---

## 一、新功能 (Features)

### 1. LLM Provider 增强

| 功能 | 提交 | 说明 |
|------|------|------|
| **VolcEngine Provider 支持** | `1663517` | 新增火山引擎 LLM 提供商，支持 coding plan endpoint |
| **SiliconFlow Provider 支持** | `80a5a8c` | 新增 SiliconFlow 提供商 |
| **GitHub Copilot OAuth 登录** | `b161fa4`, `16127d4` | 支持 GitHub Copilot OAuth 登录流程 |
| **Custom Provider 支持** | `e2a0d6` | 支持直接 OpenAI-compatible 的自定义提供商 |
| **Anthropic Prompt Caching** | `3b4763b` | 通过 `cache_control` 实现 Anthropic 提示缓存，减少 API 成本 |

### 2. Channel 增强

| 功能 | 提交 | 说明 |
|------|------|------|
| **Feishu 多媒体支持** | `d08c022` | 支持发送图片、音频和文件，通过 `im.v1.image.create` 和 `im.v1.file.create` API |
| **Telegram 消息回复** | `e1854c4`, `3ac5513` | reply-to-message 行为可配置（默认 false），支持通过 message_id 回复 |
| **Telegram 媒体处理** | `23b7e1e` | 支持处理语音消息、音频、图片、文档 |
| **Slack 线程回复** | `72db01d` | 支持 replyInThread 逻辑和自定义 react emoji |

### 3. Agent 能力增强

| 功能 | 提交 | 说明 |
|------|------|------|
| **流式中间进度** | `715b2db` | 工具执行期间向用户流式传输中间进度 |
| **ClawHub Skill** | `5735f9b` | 支持从公共注册表搜索和安装 agent skills |
| **Cron 管理** | `778a933` | CLI Agent 支持 Cron 管理 |

### 4. 部署增强

| 功能 | 提交 | 说明 |
|------|------|------|
| **Docker Compose 支持** | `c03f2b6` | 添加 `docker-compose.yml`，支持 gateway 和 CLI 服务，包含资源限制 |
| **MCP HTTP 自定义 Headers** | `c5b4331`, `f3c7337` | 支持 MCP HTTP 认证使用自定义 headers |

---

## 二、Bug 修复 (Fixes)

### 1. Agent Loop 修复

| 问题 | 提交 | 说明 |
|------|------|------|
| **模型临时文本后工具调用** | `464352c`, `1b49bf9` | MiniMax、Gemini Flash、GPT-4.1 等模型会先发送临时文本再调用工具，现在支持一次重试 |
| **内存整合重复任务** | `107a380` | 添加 `_consolidating` 集合防止同一会话重复触发整合任务 |
| **Subagent workspace 传递** | `e17342d` | 修复子代理中文件工具未接收 workspace 的问题 |

### 2. Provider 路由修复

| 问题 | 提交 | 说明 |
|------|------|------|
| **Codex Provider 路由** | `b11f0ce`, `9789307` | 优先使用显式 provider 前缀而非关键字匹配，修复 GitHub Copilot 模型路由 |
| **严格 LLM Provider 消息格式** | `e44f143`, `5cc019b` | 清理非标准键（如 `reasoning_content`），确保 assistant 消息包含 `content` 键 |

### 3. Cron 服务修复

| 问题 | 提交 | 说明 |
|------|------|------|
| **Cron run 实际执行** | `4367038`, `b97b1a5` | 修复 cron run 命令未实际执行 agent 的问题，传递完整 agent config |
| **时区验证** | `4a85cd9`, `ae903e9` | 服务层添加时区验证，修复时区显示 bug |

### 4. Shell 安全修复

| 问题 | 提交 | 说明 |
|------|------|------|
| **进程 FD 泄漏** | `c86dbc9` | Shell 超时后等待被终止的进程（5s），防止文件描述符泄漏 |
| **format 误报** | `536ed60` | 修复 URL 中 `format` 被安全守卫误判的问题（如 `curl https://wttr.in?format=3`） |

### 5. 编码与国际化修复

| 问题 | 提交 | 说明 |
|------|------|------|
| **UTF-8 编码** | `c7b5dd9`, `a2379a0` | 确保所有文件操作使用 UTF-8 编码 |
| **WhatsApp 非 ASCII 字符** | `124c611` | 发送 payload 添加 `ensure_ascii=False`，支持 CJK、emoji 等 |
| **Loguru 格式化 KeyError** | `53b83a3`, `37252a4` | 使用 loguru 原生格式化，防止消息中花括号导致 KeyError |

### 6. 其他修复

| 问题 | 提交 | 说明 |
|------|------|------|
| **Telegram /help ACL** | `33d760d` | /help 命令直接处理，绕过 ACL 检查，对所有用户可用 |
| **mcpServers 环境变量** | `4d4d629` | 修复环境变量被错误转换为 snake case 的问题 |
| **依赖版本固定** | `d748e6e` | 固定依赖版本范围，防止兼容性问题 |
| **消息上下文 message_id** | `c865b29` | 增强 message context 处理，添加 message_id 参数 |

---

## 三、重构与清理 (Refactoring)

| 内容 | 提交 | 说明 |
|------|------|------|
| **MessageBus 死代码移除** | `0001f28` | 移除未使用的 `subscribe_outbound()`, `dispatch_outbound()`, `stop()` 方法 |
| **Feishu send() 重构** | `3890f1a` | 清理 send() 方法，移除死代码 |
| **Telegram 媒体发送逻辑** | `5ad9c83` | 清理媒体发送逻辑 |
| **Session 历史获取简化** | `27a1318` | 加载时迁移旧会话，简化 get_history |
| **代码样式清理** | 多个 | 移除冗余注释、统一格式 |

---

## 四、版本发布

- **v0.1.4** (`b14d471`) - 2026-02-18 发布

---

## 五、对 NanoBot.Net 移植的影响分析

### 高优先级（需要同步实现）

1. **Agent Loop 重试机制** - 支持模型发送临时文本后再调用工具
2. **消息清理机制** - 为严格 LLM provider 清理非标准键
3. **Shell 超时处理** - 防止 FD 泄漏
4. **UTF-8 编码** - 确保国际化支持

### 中优先级（建议实现）

1. **Prompt Caching** - Anthropic 提示缓存，降低成本
2. **流式中间进度** - 提升用户体验
3. **Cron 服务增强** - 时区验证和完整配置传递

### 低优先级（可选实现）

1. **新增 Provider** - VolcEngine、SiliconFlow、GitHub Copilot
2. **Channel 增强** - Feishu 多媒体、Telegram 回复
3. **Docker Compose** - 部署支持

---

## 六、相关文件变更统计

| 模块 | 主要变更文件 |
|------|-------------|
| `nanobot/agent/` | `loop.py`, `context.py`, `subagent.py`, `tools/shell.py`, `tools/mcp.py`, `tools/cron.py` |
| `nanobot/providers/` | `litellm_provider.py`, `registry.py`, `custom_provider.py` |
| `nanobot/channels/` | `telegram.py`, `feishu.py`, `slack.py`, `whatsapp.py` |
| `nanobot/cli/` | `commands.py` |
| `nanobot/config/` | `schema.py`, `loader.py` |
| `nanobot/cron/` | `service.py` |

---

## 七、NanoBot.Net 移植版实现状态清单

> 审核时间: 2026-02-20  
> 移植版路径: `src/`  
> 原项目路径: `Temp/nanobot/`

### 1. Agent Loop 核心功能

| 功能 | 原项目实现 | 移植版实现 | 状态 | 说明 |
|------|-----------|-----------|------|------|
| **模型临时文本后工具调用重试** | `loop.py:231-244` | `AgentRuntime.cs` | ❌ 未实现 | 需要添加 `text_only_retried` 标志，支持 MiniMax/Gemini Flash 等模型 |
| **内存整合重复任务防护** | `loop.py:92, 336-345` | `MemoryConsolidator.cs` | ❌ 未实现 | 需要添加 `_consolidating` 集合防止重复触发 |
| **流式中间进度** | `loop.py:200-205` | `AgentRuntime.cs` | ❌ 未实现 | 工具执行期间向用户推送进度 |
| **工具调用日志** | `loop.py:226` | `AgentRuntime.cs` | ⚠️ 部分 | 有基础日志，缺少工具调用详情 |
| **message_id 上下文传递** | `loop.py:137-149, 347` | `MessageTools.cs` | ⚠️ 部分 | 有 metadata 传递，但 message tool 未使用 |

### 2. Shell 工具安全

| 功能 | 原项目实现 | 移植版实现 | 状态 | 说明 |
|------|-----------|-----------|------|------|
| **进程超时后等待终止** | `shell.py:84-90` | `ShellTools.cs:66-68` | ❌ 未实现 | 需要在 kill 后添加 5s 等待防止 FD 泄漏 |
| **format 命令误报修复** | `shell.py:29` | `ShellTools.cs:9` | ❌ 未实现 | 需要更新 deny pattern 为 `(?:^|[;&|]\s*)format\b` |
| **完整安全守卫** | `shell.py:118-150` | `ShellTools.cs:27-29` | ⚠️ 简化版 | 移植版只有简单 blockedCommands，缺少路径限制等 |
| **输出截断** | `shell.py:109-111` | `ShellTools.cs` | ❌ 未实现 | 需要添加 10000 字符截断 |

### 3. Provider 实现

| 功能 | 原项目实现 | 移植版实现 | 状态 | 说明 |
|------|-----------|-----------|------|------|
| **消息清理 (_sanitize_messages)** | `litellm_provider.py:155-164` | `ChatClientFactory.cs` | ❌ 未实现 | 需要清理非标准键，确保 assistant 消息有 content |
| **Anthropic Prompt Caching** | `litellm_provider.py:111-142` | `ChatClientFactory.cs` | ❌ 未实现 | 需要添加 `cache_control` 支持 |
| **VolcEngine Provider** | `registry.py` | `ChatClientFactory.cs` | ❌ 未实现 | 新增火山引擎支持 |
| **SiliconFlow Provider** | `registry.py` | `ChatClientFactory.cs` | ❌ 未实现 | 新增 SiliconFlow 支持 |
| **GitHub Copilot OAuth** | `commands.py`, `litellm_provider.py` | - | ❌ 未实现 | 需要 OAuth 登录流程 |
| **Custom Provider** | `custom_provider.py` | `ChatClientFactory.cs` | ⚠️ 部分 | 可通过 apiBase 配置，但无专用实现 |
| **Provider 前缀路由** | `litellm_provider.py:81-109` | `ChatClientFactory.cs:109-118` | ✅ 已实现 | 有 LiteLLMPrefix 机制 |

### 4. Channel 实现

| 功能 | 原项目实现 | 移植版实现 | 状态 | 说明 |
|------|-----------|-----------|------|------|
| **Telegram reply-to-message** | `telegram.py` 配置项 | `TelegramChannel.cs` | ❌ 未实现 | 需要添加 `replyToMessage` 配置和 `reply_to_message_id` |
| **Telegram 媒体处理** | `telegram.py:23b7e1e` | `TelegramChannel.cs:156-165` | ⚠️ 部分 | 有媒体检测，但未下载/处理实际文件 |
| **Telegram /help ACL 绕过** | `telegram.py:33d760d` | `TelegramChannel.cs` | ❌ 未实现 | /help 应直接处理，不经过 ACL |
| **Feishu 多媒体发送** | `feishu.py:d08c022` | `FeishuChannel.cs` | ❌ 未实现 | 需要添加图片/音频/文件上传 API |
| **Slack 线程回复** | `slack.py:72db01d` | `SlackChannel.cs:167-169, 282-293` | ✅ 已实现 | 有 thread_ts 和 replyInThread 逻辑 |
| **Slack 自定义 react emoji** | `slack.py` 配置项 | `SlackChannel.cs:175-184` | ⚠️ 部分 | 固定使用 "eyes"，无配置项 |
| **WhatsApp 非 ASCII 支持** | `whatsapp.py:124c611` | `WhatsAppChannel.cs` | ⚠️ 待验证 | 需确认 JSON 序列化是否正确处理 Unicode |

### 5. Cron 服务

| 功能 | 原项目实现 | 移植版实现 | 状态 | 说明 |
|------|-----------|-----------|------|------|
| **时区验证** | `service.py:4a85cd9` | `CronService.cs` | ❌ 未实现 | 需要添加 `_validate_schedule_for_add()` |
| **完整 agent config 传递** | `commands.py:b97b1a5` | `CronCommand.cs` | ⚠️ 待验证 | 需确认 mcp_servers 等配置传递 |
| **Cron run 实际执行** | `commands.py:4367038` | `CronCommand.cs` | ⚠️ 待验证 | 需确认 onJobCallback 是否正确执行 agent |

### 6. MCP 实现

| 功能 | 原项目实现 | 移植版实现 | 状态 | 说明 |
|------|-----------|-----------|------|------|
| **HTTP 自定义 Headers** | `mcp.py:c5b4331` | `McpClient.cs`, `McpServerConfig.cs` | ❌ 未实现 | 需要添加 `headers` 配置项 |
| **环境变量保留** | `loader.py:4d4d629` | `McpClient.cs:41-44` | ✅ 已实现 | Env 字典正确传递 |

### 7. 编码与国际化

| 功能 | 原项目实现 | 移植版实现 | 状态 | 说明 |
|------|-----------|-----------|------|------|
| **UTF-8 文件编码** | `loop.py`, `loader.py` 等 | 各文件 | ✅ 已实现 | .NET 默认 UTF-8，File.ReadAllTextAsync 等 |
| **JSON 非 ASCII 字符** | `ensure_ascii=False` | `System.Text.Json` | ✅ 已实现 | 默认不转义非 ASCII |

### 8. Subagent 实现

| 功能 | 原项目实现 | 移植版实现 | 状态 | 说明 |
|------|-----------|-----------|------|------|
| **Workspace 传递** | `subagent.py:e17342d` | `SpawnTool.cs:77-133` | ✅ 已实现 | workspace 正确传递给 subagent |

---

### 实现状态汇总

| 类别 | 已实现 | 部分实现 | 未实现 | 完成率 |
|------|--------|---------|--------|--------|
| Agent Loop | 1 | 2 | 3 | 17% |
| Shell 工具 | 0 | 1 | 3 | 0% |
| Provider | 1 | 1 | 6 | 14% |
| Channel | 1 | 4 | 3 | 20% |
| Cron 服务 | 0 | 2 | 1 | 0% |
| MCP | 1 | 0 | 1 | 50% |
| 编码国际化 | 2 | 0 | 0 | 100% |
| Subagent | 1 | 0 | 0 | 100% |
| **总计** | **7** | **10** | **17** | **26%** |

---

### 优先修复建议

#### 🔴 高优先级（影响核心功能）

1. **Agent Loop 重试机制** - 影响 MiniMax、Gemini Flash 等模型使用工具
2. **Shell FD 泄漏修复** - 长时间运行可能导致资源耗尽
3. **消息清理机制** - 严格 Provider（如 StepFun）会返回 400 错误

#### 🟡 中优先级（影响用户体验）

4. **内存整合重复防护** - 防止 API 调用风暴
5. **流式中间进度** - 提升工具执行时的用户体验
6. **Telegram /help ACL 绕过** - 影响未授权用户使用帮助

#### 🟢 低优先级（功能增强）

7. **新增 Provider 支持** - VolcEngine、SiliconFlow 等
8. **Channel 多媒体增强** - Feishu 图片/文件发送
9. **MCP HTTP Headers** - 支持自定义认证

---

**报告生成时间**: 2026-02-20
